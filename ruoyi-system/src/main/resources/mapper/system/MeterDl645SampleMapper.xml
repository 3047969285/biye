<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.MeterDl645SampleMapper">

    <resultMap type="com.ruoyi.system.domain.MeterDl645Sample" id="MeterDl645SampleResult">
        <id property="id" column="id"/>
        <result property="meterAddress" column="meter_address"/>
        <result property="diCode" column="di_code"/>
        <result property="pointCode" column="point_code"/>
        <result property="pointName" column="point_name"/>
        <result property="value" column="value_num"/>
        <result property="unit" column="unit"/>
        <result property="readTime" column="read_time"/>
        <result property="source" column="source"/>
        <result property="rawFrameHex" column="raw_frame_hex"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="selectVo">
        select id, meter_address, di_code, point_code, point_name, value_num, unit, read_time, source, raw_frame_hex,
               create_by, create_time
        from meter_dl645_sample
    </sql>

    <insert id="insertMeterDl645Sample" useGeneratedKeys="true" keyProperty="id">
        insert into meter_dl645_sample (
            meter_address, di_code, point_code, point_name, value_num, unit, read_time, source, raw_frame_hex, create_by, create_time
        ) values (
            #{meterAddress}, #{diCode}, #{pointCode}, #{pointName}, #{value}, #{unit}, #{readTime}, #{source}, #{rawFrameHex}, #{createBy}, #{createTime}
        )
    </insert>

    <select id="selectMeterDl645SampleList" resultMap="MeterDl645SampleResult">
        <include refid="selectVo"/>
        <where>
            1 = 1
            <if test="meterAddress != null and meterAddress != ''">and meter_address = #{meterAddress}</if>
            <if test="diCode != null and diCode != ''">and di_code = #{diCode}</if>
            <if test="pointCode != null and pointCode != ''">and point_code = #{pointCode}</if>
        </where>
        order by read_time desc, id desc
    </select>

    <select id="selectLatestByRange" resultMap="MeterDl645SampleResult">
        <include refid="selectVo"/>
        <where>
            1 = 1
            <if test="beginTime != null">and read_time &gt;= #{beginTime}</if>
            <if test="endTime != null">and read_time &lt;= #{endTime}</if>
        </where>
        order by read_time desc, id desc
    </select>
</mapper>

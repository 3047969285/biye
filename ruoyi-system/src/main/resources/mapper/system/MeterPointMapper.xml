<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.MeterPointMapper">

    <resultMap type="com.ruoyi.system.domain.MeterPoint" id="MeterPointResult">
        <id property="pointId" column="point_id"/>
        <result property="pointCode" column="point_code"/>
        <result property="pointName" column="point_name"/>
        <result property="slaveId" column="slave_id"/>
        <result property="addrOffset" column="addr_offset"/>
        <result property="registerType" column="register_type"/>
        <result property="dataType" column="data_type"/>
        <result property="wordSwap" column="word_swap"/>
        <result property="scale" column="scale"/>
        <result property="valueOffset" column="value_offset"/>
        <result property="unit" column="unit"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectMeterPointVo">
        select point_id, point_code, point_name, slave_id, addr_offset, register_type, data_type, word_swap,
               scale, value_offset, unit, sort_order, status, del_flag, create_by, create_time, update_by, update_time, remark
        from meter_point
    </sql>

    <select id="selectMeterPointById" resultMap="MeterPointResult">
        <include refid="selectMeterPointVo"/>
        where point_id = #{pointId}
    </select>

    <select id="selectByPointCode" resultMap="MeterPointResult">
        <include refid="selectMeterPointVo"/>
        where point_code = #{pointCode} and del_flag = '0'
    </select>

    <select id="selectMeterPointList" resultMap="MeterPointResult">
        <include refid="selectMeterPointVo"/>
        <where>
            del_flag = '0'
            <if test="pointCode != null and pointCode != ''">and point_code like concat('%', #{pointCode}, '%')</if>
            <if test="pointName != null and pointName != ''">and point_name like concat('%', #{pointName}, '%')</if>
            <if test="slaveId != null">and slave_id = #{slaveId}</if>
            <if test="status != null and status != ''">and status = #{status}</if>
        </where>
        order by sort_order asc, point_id asc
    </select>

    <select id="selectActivePoints" resultMap="MeterPointResult">
        <include refid="selectMeterPointVo"/>
        where del_flag = '0' and status = '0'
        order by sort_order asc, point_id asc
    </select>

    <insert id="insertMeterPoint" useGeneratedKeys="true" keyProperty="pointId">
        insert into meter_point (
            point_code, point_name, slave_id, addr_offset, register_type, data_type, word_swap,
            scale, value_offset, unit, sort_order, status, del_flag, create_by, create_time, remark
        ) values (
            #{pointCode}, #{pointName}, #{slaveId}, #{addrOffset}, #{registerType}, #{dataType}, #{wordSwap},
            #{scale}, #{valueOffset}, #{unit}, #{sortOrder}, #{status}, #{delFlag}, #{createBy}, #{createTime}, #{remark}
        )
    </insert>

    <insert id="insertMeterPointBatch">
        insert into meter_point (
            point_code, point_name, slave_id, addr_offset, register_type, data_type, word_swap,
            scale, value_offset, unit, sort_order, status, del_flag, create_by, create_time, remark
        ) values
        <foreach collection="list" item="item" separator=",">
            (#{item.pointCode}, #{item.pointName}, #{item.slaveId}, #{item.addrOffset}, #{item.registerType},
             #{item.dataType}, #{item.wordSwap}, #{item.scale}, #{item.valueOffset}, #{item.unit}, #{item.sortOrder},
             #{item.status}, #{item.delFlag}, #{item.createBy}, #{item.createTime}, #{item.remark})
        </foreach>
    </insert>

    <update id="updateMeterPoint">
        update meter_point
        <set>
            <if test="pointCode != null">point_code = #{pointCode},</if>
            <if test="pointName != null">point_name = #{pointName},</if>
            <if test="slaveId != null">slave_id = #{slaveId},</if>
            <if test="addrOffset != null">addr_offset = #{addrOffset},</if>
            <if test="registerType != null">register_type = #{registerType},</if>
            <if test="dataType != null">data_type = #{dataType},</if>
            <if test="wordSwap != null">word_swap = #{wordSwap},</if>
            <if test="scale != null">scale = #{scale},</if>
            <if test="valueOffset != null">value_offset = #{valueOffset},</if>
            <if test="unit != null">unit = #{unit},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        where point_id = #{pointId}
    </update>

    <delete id="deleteMeterPointById">
        update meter_point set del_flag = '2' where point_id = #{pointId}
    </delete>

    <delete id="deleteMeterPointByIds">
        update meter_point set del_flag = '2' where point_id in
        <foreach collection="array" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>
</mapper>

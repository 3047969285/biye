<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qcl.good.mapper.GoodTypeMapper">
    
    <resultMap type="com.qcl.good.domain.GoodType" id="GoodTypeResult">
        <result property="typeId"    column="type_id"    />
        <result property="typeName"    column="type_name"    />
    </resultMap>

    <resultMap id="GoodTypeGoodResult" type="com.qcl.good.domain.GoodType" extends="GoodTypeResult">
        <collection property="goodList" ofType="com.qcl.good.domain.Good" column="type_id" select="selectGoodList" />
    </resultMap>

    <resultMap type="com.qcl.good.domain.Good" id="GoodResult">
        <result property="goodId"    column="good_id"    />
        <result property="goodName"    column="good_name"    />
        <result property="price"    column="price"    />
        <result property="imageUrl"    column="image_url"    />
        <result property="typeId"    column="type_id"    />
    </resultMap>

    <sql id="selectGoodTypeVo">
        select type_id, type_name from good_type
    </sql>

    <select id="selectGoodTypeList" parameterType="com.qcl.good.domain.GoodType" resultMap="GoodTypeResult">
        <include refid="selectGoodTypeVo"/>
        <where>  
            <if test="typeName != null  and typeName != ''"> and type_name like concat('%', #{typeName}, '%')</if>
        </where>
    </select>
    
    <select id="selectGoodTypeByTypeId" parameterType="java.lang.Long" resultMap="GoodTypeGoodResult">
        select type_id, type_name
        from good_type
        where type_id = #{typeId}
    </select>

    <select id="selectGoodList" resultMap="GoodResult">
        select good_id, good_name, price, image_url, type_id
        from good
        where type_id = #{type_id}
    </select>

    <insert id="insertGoodType" parameterType="com.qcl.good.domain.GoodType" useGeneratedKeys="true" keyProperty="typeId">
        insert into good_type
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="typeName != null and typeName != ''">type_name,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="typeName != null and typeName != ''">#{typeName},</if>
         </trim>
    </insert>

    <update id="updateGoodType" parameterType="com.qcl.good.domain.GoodType">
        update good_type
        <trim prefix="SET" suffixOverrides=",">
            <if test="typeName != null and typeName != ''">type_name = #{typeName},</if>
        </trim>
        where type_id = #{typeId}
    </update>

    <delete id="deleteGoodTypeByTypeId" parameterType="java.lang.Long">
        delete from good_type where type_id = #{typeId}
    </delete>

    <delete id="deleteGoodTypeByTypeIds" parameterType="java.lang.String">
        delete from good_type where type_id in 
        <foreach item="typeId" collection="array" open="(" separator="," close=")">
            #{typeId}
        </foreach>
    </delete>
    
    <delete id="deleteGoodByTypeIds" parameterType="java.lang.String">
        delete from good where type_id in 
        <foreach item="typeId" collection="array" open="(" separator="," close=")">
            #{typeId}
        </foreach>
    </delete>

    <delete id="deleteGoodByTypeId" parameterType="java.lang.Long">
        delete from good where type_id = #{typeId}
    </delete>

    <insert id="batchGood">
        insert into good( good_id, good_name, price, image_url, type_id) values
        <foreach item="item" index="index" collection="list" separator=",">
            ( #{item.goodId}, #{item.goodName}, #{item.price}, #{item.imageUrl}, #{item.typeId})
        </foreach>
    </insert>
</mapper>
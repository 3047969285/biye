from pathlib import Path
from copy import deepcopy
from docx import Document
from docx.text.paragraph import Paragraph

ROOT = Path.home() / "Desktop"
SRC = ROOT / "王昌振毕业论文v1.0_目录对齐_40页左右.docx"
OUT = ROOT / "王昌振毕业论文v1.0_50页扩写版_未校页码.docx"

def insert_after(paragraph: Paragraph, text: str, style_name: str | None = None) -> Paragraph:
    new_p = deepcopy(paragraph._p)
    for child in list(new_p):
        new_p.remove(child)
    paragraph._p.addnext(new_p)
    new_para = Paragraph(new_p, paragraph._parent)
    if style_name:
        new_para.style = style_name
    new_para.add_run(text)
    return new_para

def find_start(doc, prefix):
    for p in doc.paragraphs:
        if p.text.strip().startswith(prefix):
            return p
    raise ValueError(prefix)

def insert_many_after(p, texts):
    cur = p
    for t in texts:
        cur = insert_after(cur, t, "Normal")
    return cur

def main():
    doc = Document(str(SRC))
    # Chapter 4 expansions: each section keeps design and implementation separated.
    insert_many_after(find_start(doc, "4.1  设备"), [
        "详细设计方面，设备模块首先需要解决数据口径统一的问题。毕业设计中涉及设备台账、设备状态、告警、工单和预测结果，如果每个模块都单独保存设备名称和位置，后续修改设备信息时就容易出现不一致。因此本系统把 eq_device 作为设备主数据表，其他业务表只保存设备编号或设备主键，页面展示时再按关联关系取出设备名称、类型和安装位置。",
        "详细设计方面，状态数据没有直接合并到设备表中，是因为设备基础信息变化较少，而运行状态会不断增加。把二者拆开后，设备列表查询不会因为状态记录增加而变慢，状态趋势查询也可以单独按设备编号和时间范围建立条件。这个设计在开发时比较实用，既方便写列表接口，也方便后续接入真实采集数据。",
        "具体实现方面，设备新增和修改接口沿用 RuoYi 的统一返回格式，前端提交表单后由后端完成字段校验和业务处理。设备状态写入时，Service 层先补充创建时间、设备编号等信息，再调用 Mapper 落库。对于可能影响多张表的数据操作，使用事务保证失败回滚，避免出现设备删除成功但关联数据仍然残留的情况。",
        "具体实现方面，前端页面没有把所有字段一次性堆在列表里，而是把常用字段放在列表页，把较详细的运行参数放在详情页。这样页面看起来更清楚，也符合实际使用习惯。运维人员先通过名称、类型或区域筛选设备，再进入详情查看状态曲线和历史记录，操作路径比较自然。",
    ])
    insert_many_after(find_start(doc, "4.2 规则"), [
        "详细设计方面，规则告警模块的重点是把“异常判断”从普通数据维护中分离出来。状态表只负责记录事实，规则表负责表达判断标准，告警表负责保存判断结果。这样的三层关系比较清楚：状态数据可以长期保存，规则可以按需要调整，告警则记录某一次异常事件。",
        "详细设计方面，规则字段需要兼顾简单和可扩展。本系统主要围绕阈值型规则实现，例如某项指标高于或低于设定值后触发告警。虽然目前没有实现复杂规则表达式，但在服务层保留了规则评估入口，后续如果增加连续多次异常、时间窗口判断或多指标组合判断，可以继续在评估服务中扩展。",
        "具体实现方面，告警生成不是直接在 Controller 中完成，而是由触发服务调用评估服务，再由告警服务写入记录。这样做的好处是在不同入口都可以复用同一套规则判断逻辑，例如手动录入状态、定时采集状态或者后续网关上报状态，都可以进入同一个评估流程。",
        "具体实现方面，告警消息生成后会进入站内消息表，前端通过消息中心展示未读数量。这个处理虽然不复杂，但对系统体验影响比较明显。因为运维人员不一定一直停留在告警页面，消息提醒可以让异常更容易被注意到，也让告警模块和工单模块之间的衔接更紧密。",
    ])
    insert_many_after(find_start(doc, "4.3 对话"), [
        "详细设计方面，AI 功能没有直接和设备管理代码混在一起，而是放在 spring-ai 模块中集中处理。这样普通业务模块仍然保持传统的增删改查结构，AI 模块则专门负责模型调用、知识库检索、自然语言转 SQL 和对话历史记录。模块边界清楚后，后续替换模型或调整提示词时，不会影响设备、告警和工单等基础功能。",
        "详细设计方面，RAG 问答的核心不是让模型自由发挥，而是让模型先看资料再回答。系统把上传文档切分成较小片段，保存到向量库中；用户提问时先检索相似片段，再把片段和问题一起发送给模型。这样回答更容易贴近论文和项目资料，也更适合电力运维这种需要依据和步骤的场景。",
        "详细设计方面，自然语言查库单独设置安全边界。用户可以用自然语言提出查询需求，但最终是否执行 SQL 由后端决定。系统只允许查询语句通过，不允许模型生成更新、删除、建表等危险操作。这个设计在答辩时也比较容易说明：AI 负责辅助生成，系统负责最终把关。",
        "具体实现方面，普通对话和知识库问答都采用流式返回。前端通过 EventSource 建立连接，后端使用 SseEmitter 按片段推送模型输出。这样即使模型回答较长，页面也能不断显示新内容，不会让用户一直等待空白页面。对话结束后，系统再把完整回答写入 ai_chat_record，方便后续查看。",
        "具体实现方面，知识库上传时需要处理不同格式的文档。TXT 可以直接读取，PDF 需要提取文本，DOCX 需要按段落解析。解析后的内容会过滤空行和无意义字符，再按长度切分成多个片段。这个过程看起来偏底层，但决定了后续问答质量，如果切分过长会影响检索精度，切分过短又容易丢失上下文。",
        "具体实现方面，数据库问答返回结果时采用表格展示，而不是只返回一段文字说明。这样用户可以直接看到查询字段和数据内容，也便于判断回答是否可信。对于模型生成失败、SQL 校验不通过或查询为空等情况，系统会返回明确提示，避免把底层异常直接暴露给用户。",
    ])
    insert_many_after(find_start(doc, "4.4 运维"), [
        "详细设计方面，工单模块承担的是异常处理闭环。告警只是说明系统发现了问题，但问题是否已经分派、谁来处理、处理结果是什么，还需要通过工单记录下来。因此工单表中保存负责人、问题描述、状态、处理意见和时间节点等字段，保证一次异常处理过程可以被追踪。",
        "详细设计方面，工单状态不能随意跳转。如果允许未分派工单直接完成，或者允许已完成工单继续修改处理内容，系统记录就会失去可信度。因此本系统在服务层对状态流转做了限制，要求工单按照创建、分派、处理、审核、完成等顺序推进。",
        "详细设计方面，风电预测模块采用跨语言服务设计。Java 后台擅长权限、接口、数据库和页面集成，Python 更适合模型训练和推理。把预测能力放在 FastAPI 服务中，可以让 GRU 模型独立维护；Java 侧只关心请求参数和返回结果，降低了两个技术栈之间的耦合。",
        "具体实现方面，预测接口调用前需要准备设备、预测长度和时间范围等参数。Java 后端通过 HTTP 请求访问 Python 服务，Python 读取模型文件和特征数据后返回预测功率数组、真实功率数组以及误差指标。后端拿到结果后先判断 success 字段，再决定是否写入预测表和返回前端。",
        "具体实现方面，预测曲线使用 ECharts 展示。页面上同时显示预测功率和真实功率，能够直观看到模型输出与历史数据之间的差异。对于毕业设计来说，这种可视化比单纯展示数字更直观，也更容易说明模型服务已经接入到管理平台中。",
        "具体实现方面，外部预测服务可能出现未启动、路径错误或模型加载失败等情况。系统在后端增加异常捕获和可读提示，避免页面一直加载。这样即使预测服务暂时不可用，设备管理、告警、工单和消息等基础功能仍然可以继续使用。",
    ])
    insert_many_after(find_start(doc, "4.5 消息"), [
        "详细设计方面，消息模块用于补足系统的主动提醒能力。仅仅把告警和工单放在列表中是不够的，因为用户可能没有一直刷新页面。通过站内消息记录提醒内容、接收用户、已读状态和创建时间，可以让异常提醒和待办事项更容易被发现。",
        "详细设计方面，权限控制采用前后端结合方式。前端根据权限标识控制按钮显示，能够减少普通用户误操作；后端通过注解再次校验接口权限，防止用户绕过页面直接请求接口。前端控制体验，后端控制安全，二者缺一不可。",
        "具体实现方面，消息表统一保存告警提醒、工单提醒和系统通知。用户登录后，前端可以查询未读消息数量，并在消息中心查看详情。消息已读后更新状态，避免同一条提醒反复出现。后续如果要接入 WebSocket 或其他推送方式，也可以继续复用这张消息表。",
        "具体实现方面，工程化处理主要体现在统一异常、操作日志、事务控制和接口权限。比如删除设备、调整规则、审核工单等操作都属于关键行为，需要留下记录。开发阶段这些日志方便排查问题，后续如果接入真实环境，也能作为审计依据。",
    ])
    # Chapter 5 expansions: test process and figure explanations.
    insert_many_after(find_start(doc, "5.1 测试"), [
        "测试阶段主要按照真实使用顺序进行，而不是只单独点击页面。我的测试顺序是先验证设备台账能否维护，再验证状态数据和告警规则能否联动，然后测试 AI 问答、知识库问答和数据库问答，最后检查工单、消息和预测功能。这样可以看出各模块之间是否真的连起来。",
        "测试数据以项目已有数据和手动录入数据为主。设备信息用于验证台账管理，异常状态用于触发告警，运维资料用于验证 RAG 检索，预测样本用于验证 GRU 服务调用。虽然这些数据还不是现场实时采集数据，但已经能够覆盖毕业设计中实现的主要流程。",
        "测试时除了记录成功截图，也会观察失败提示。例如必填项为空时是否提示，数据库问答生成非查询语句时是否拒绝执行，预测服务不可用时页面是否给出说明。这样做是为了避免系统只在理想输入下可用，而在异常场景下没有反馈。",
    ])
    figure_additions = {
        "图5.1.1": "从图5.1.1可以看到，设备修改后页面能够重新显示最新字段。这个测试说明编辑表单、后端更新接口和列表刷新之间能够衔接，适合日常维护设备基础资料。",
        "图5.1.2": "图5.1.2展示新增设备后的结果。新增功能是后续告警、工单和预测的基础，如果设备主数据不能正确录入，其他模块就无法稳定关联到具体对象。",
        "图5.1.3": "图5.1.3中回答内容以流式方式返回，说明 SSE 链路能够正常工作。相比一次性返回，流式输出可以降低等待感，也更符合对话类页面的交互习惯。",
        "图5.1.4": "图5.1.4体现了知识库问答和普通问答的区别。系统回答不是只依靠通用模型知识，而是结合上传资料进行组织，因此更适合回答项目文档和运维资料中的具体问题。",
        "图5.1.5": "图5.1.5验证了自然语言查库功能。该功能的重点不只是生成 SQL，还包括后端安全校验和结果表格化展示，避免用户直接接触数据库细节。",
        "图5.1.6": "图5.1.6中的预测曲线说明 Python 模型服务已经被 Java 平台调用成功。该图也是本项目比较有特点的测试结果，因为它把管理系统和深度学习模型连接在了一起。",
        "图5.1.7": "图5.1.7展示工单生成后进入流程管理。测试时重点关注状态是否按顺序变化，以及负责人、处理意见等字段是否能够保存，确保工单不是简单备注。",
        "图5.1.8": "图5.1.8说明消息中心可以展示提醒内容。消息模块虽然实现难度不如 AI 和预测高，但它直接影响运维人员是否能及时发现待处理事项。",
        "图5.1.9": "图5.1.9展示告警记录列表。该测试验证规则触发后的最终结果是否能落到页面上，也说明状态数据、规则服务和告警页面之间的链路是可用的。",
    }
    for cap, text in figure_additions.items():
        insert_many_after(find_start(doc, cap), [text])
    insert_many_after(find_start(doc, "5.2 性能"), [
        "性能测试以单机开发环境下的可用性观察为主。设备列表、告警列表和工单列表都采用分页查询，测试时连续切换页码、组合查询条件和刷新页面，系统能够正常返回结果，没有出现明显卡顿。",
        "AI 功能的响应时间与外部模型接口有关，因此测试时更关注前端是否能持续接收内容。普通对话和知识库问答在较长回答场景下能够逐段显示，说明流式输出对改善体验有一定作用。",
        "预测模块的性能主要受 Python 服务和模型推理影响。使用已训练模型进行预测时，页面能够在可接受时间内展示曲线；如果后续加入在线训练，则应将训练任务放到后台执行，并增加进度提示。",
        "兼容性方面主要在 Edge 和 Chrome 中验证。设备管理、AI 问答、知识库检索、数据库问答、预测图表和消息中心都能正常显示，说明当前前端实现可以满足常见桌面浏览器访问。",
    ])
    insert_many_after(find_start(doc, "5.3 局限"), [
        "从测试结果看，系统功能链路已经比较完整，但测试条件仍然有限。当前数据主要来自仿真数据、历史样本和人工录入，没有接入真实采集网关，因此测试结论更适合作为原型系统验证。",
        "AI 模块仍然依赖在线模型服务。网络不稳定或模型接口不可用时，问答功能会受到影响。虽然系统增加了异常提示，但还没有实现完全离线的兜底能力，这是后续需要继续完善的方向。",
        "预测模块目前主要展示趋势辅助判断，不能直接替代专业调度决策。GRU 模型效果会受到训练数据、气象条件和设备状态影响，后续需要接入更多真实特征并持续评估误差。",
    ])
    doc.save(str(OUT))
    print(OUT)
    print('chars', sum(len(p.text) for p in doc.paragraphs), 'paragraphs', len(doc.paragraphs), 'images', len(doc.inline_shapes), 'tables', len(doc.tables))

if __name__ == '__main__':
    main()
